import React from "react";

const Controls = () => {
  return (
    <div className="gap-2 flex">
      <button className="border text-black">Rotate left</button>
      <button className="border text-black">Forward</button>
      <button className="border text-black">Rotate right</button>
    </div>
  );
};

export default Controls;
